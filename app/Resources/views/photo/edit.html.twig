{% extends 'base.html.twig' %}

{% block h2 %}
    <h2 class="title">Edycja zdjęcia nr {{ photo.id }}</h2>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-6">
            {{ form_start(edit_form) }}
            <div class="custom-file mb-3">
                {{ form_widget(edit_form) }}
            </div>
            <input type="submit" value="Edytuj" class="btn btn-success mb-3">
            {{ form_end(edit_form) }}

            <div>
                <a href="{{ path('_dashboard') }}" class="btn btn-danger mb-3"><i class="fas fa-chevron-left fa-mr"></i>Cofnij</a>
            </div>
        </div>
    </div>

{% endblock %}
