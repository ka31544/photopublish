{% extends 'base.html.twig' %}

{% block h2 %}
    <h2 class="title">Dodaj zdjęcie</h2>
{% endblock %}

{% block body %}

<div class="row">
    <div class="col-lg-6">
    {{ form_start(form) }}
        <div class="custom-file mb-3">
            {{ form_row(form.src) }}
        </div>
        {{ form_row(form._token) }}
        <input type="submit" value="Dodaj" class="btn btn-success">
    {{ form_end(form, {'render_rest': false}) }}

    {#{{ app.request.headers.get('referer') }}#}
        <a href="{{ path('_dashboard') }}" class="btn btn-danger mt-3"><i class="fas fa-chevron-left fa-mr"></i>Cofnij</a>
    </div>
</div>
{% endblock %}
